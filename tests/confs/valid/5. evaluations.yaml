job:
  KIND: EvaluationsJob
  inputs:
    KIND: ParquetReader
    path: "${tests_path:}/data/inputs_sample.parquet"
    limit: 1500
  targets:
    KIND: ParquetReader
    path: "${tests_path:}/data/targets_sample.parquet"
    limit: 1500
  # CLI smoke test only; threshold acceptance/rejection is covered in job tests.
  thresholds: {}
